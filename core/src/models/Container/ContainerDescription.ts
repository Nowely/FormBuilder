import {AllPropsDescriptionOf} from "../../utils/constants";
import {IContainer} from "./IContainer";

export const ContainerDescription: AllPropsDescriptionOf<IContainer> = {
    key: null,
    children: null
}